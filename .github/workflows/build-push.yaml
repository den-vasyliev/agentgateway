name: Build and Push Image

on:
  push:
    branches:
      - gemini
  workflow_dispatch:

env:
  REGISTRY_IMAGE: ghcr.io/${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        run: |
          make docker DOCKER_REGISTRY=ghcr.io DOCKER_REPO=${{ github.repository_owner }}
          docker push ghcr.io/${{ github.repository_owner }}/agentgateway:$(git describe --tags --always --dirty)